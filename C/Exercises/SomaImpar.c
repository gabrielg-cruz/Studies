#include <stdio.h>

int main (){

	int x, y, soma;
	int i = y;
	scanf("%d", &x);
	scanf("%d", &y);
	
	for(i; i < x; i++){
		
		soma = i + (y + 2);
		printf("%d", soma);
	}
	
	return 0;
}
